<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="Content-Security-Policy" content="connect-src 'self' https://www.google-analytics.com;">
    <title>Forgot Password</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Include your stylesheet here -->

    <style>
        /* Same CSS from your code */
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(to right, #1e3c72, #2a5298);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            color: white;
        }

        .auth-container {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            padding: 35px;
            border-radius: 12px;
            width: 380px;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.3);
            text-align: center;
            transition: width 0.3s ease, padding 0.3s ease;
        }

        .auth-title {
            font-size: 30px;
            text-align: center;
            margin-bottom: 20px;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: #ffcc00;
        }

        .input-group {
            margin-bottom: 18px;
            text-align: left;
        }

        .input-group label {
            display: block;
            font-size: 14px;
            margin-bottom: 6px;
            font-weight: bold;
            color: #ffcc00;
        }

        .input-group input {
            width: 100%;
            padding: 12px;
            border: none;
            border-radius: 6px;
            font-size: 15px;
            background: rgba(255, 255, 255, 0.9);
            color: #333;
            transition: 0.3s;
        }

        .input-group input:focus {
            outline: none;
            background: white;
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.6);
        }

        .auth-button {
            background: #ff9900;
            color: white;
            font-size: 17px;
            border: none;
            padding: 12px;
            width: 100%;
            border-radius: 6px;
            cursor: pointer;
            transition: 0.3s;
            font-weight: bold;
            text-transform: uppercase;
        }

        .auth-button:hover {
            background: #e68a00;
            transform: scale(1.05);
        }

        .switch-text {
            margin-top: 18px;
            font-size: 15px;
        }

        .switch-text a {
            color: #ffcc00;
            font-weight: bold;
            text-decoration: none;
            transition: 0.3s;
        }

        .switch-text a:hover {
            text-decoration: underline;
        }

        .success-message {
            margin-top: 20px;
            font-size: 16px;
            color: #4CAF50; /* Green color for success */
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="auth-container">
        <h1 class="auth-title">Forgot Password</h1>
        <% if (successMessage) { %>
            <p class="success-message"><%= successMessage %></p>
        <% } else { %>
            <form action="/send-reset-link" method="POST" class="auth-form">
                <div class="input-group">
                    <label for="email">Enter your email:</label>
                    <input type="email" id="email" name="email" required placeholder="Your email address">
                </div>
                <button type="submit" class="auth-button">Send Reset Link</button>
            </form>
        <% } %>
        <p class="switch-text">Remember your password? <a href="/login">Login here</a></p>
    </div>
</body>
</html>